import numpy as np

# Background color of the simulation
BACKGROUND_COLOR = np.array([0, 0, 0])

# Base position of the panda robot
ROBOT_BASE_POSITION = np.array([-0.1, 0.0, 0.0])

# End effector position identifier color
EE_POS_IDENTIFIER_COLOR = np.array([1, 0, 0, 0.3])

# Waypoint color
WAYPOINT_COLOR = np.array([1, 1, 0.0, 1.0])

# Safe region color
SAFE_REGION_COLOR = np.array([1, 0, 0, 0.3])

# Tight region color
TIGHT_REGION_COLOR = np.array([0, 1, 0, 0.3])

# Release region color
RELEASE_REGION_COLOR = np.array([0, 0, 1, 0.3])

# Rail color
RAIL_COLOR = np.array([1, 1, 0, 1])

# Conveyor belt colour
CONVEYOR_BELT_COLOR = np.array([0.5, 0.5, 0.5, 1.0])
